#if !defined(GRAINS_H)
#define GRAINS_H

#include <cstdint>
#include <stdexcept>

namespace grains {
    std::uint64_t square(std::uint8_t s);
    std::uint64_t total();
}  // namespace grains

#endif // GRAINS_H
